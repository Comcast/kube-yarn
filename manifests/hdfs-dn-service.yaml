kind: Service
apiVersion: v1
metadata:
  name: hdfs-dn
  namespace: yarn-cluster
spec:
  type: ClusterIP
  sessionAffinity: ClientIP
  selector:
    component: hdfs-dn
  ports:
  - name: dfs
    port: 9000
    protocol: TCP
