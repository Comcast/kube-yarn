kind: Service
apiVersion: v1
metadata:
  name: hdfs-datanode
  namespace: yarn-cluster
spec:
  type: ClusterIP
  sessionAffinity: ClientIP
  selector:
    component: hdfs-datanode
  ports:
  - name: dfs
    port: 9000
    protocol: TCP
