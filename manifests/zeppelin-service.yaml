kind: Service
apiVersion: v1
metadata:
  name: zeppelin
  namespace: yarn-cluster
spec:
  type: NodePort
  selector:
    component: zeppelin
  ports:
  - name: notebook
    port: 8080
    protocol: TCP
